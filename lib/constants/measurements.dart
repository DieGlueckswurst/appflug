import 'package:flutter/material.dart';

const double sidePadding = 30;
const double touchTargetForButtons = 55;
final BorderRadius borderRadius = BorderRadius.circular(10.0);
final BoxConstraints webMaxWidthConstraint = BoxConstraints(maxWidth: 400);
